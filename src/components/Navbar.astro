---
import PageSection from "./PageSection.astro";
interface Props {
    title: string;
}

const { title } = Astro.props;
const links = [
    {
        href: '/',
        i: '/',
        label: 'Home'
    },
    {
        href: '/about/',
        i: '/about',
        label: 'About'
    },
    {
        href: '/posts/',
        i: '/posts',
        label: 'Posts'
    },
    {
        href: '/photography/',
        i: '/photography',
        label: 'Photography'
    },
    {
        href: '/projects/',
        i: '/projects',
        label: 'Projects'
    },
    {
        href: '/contact/',
        i: '/contact',
        label: 'Contact'
    }
]
const url = Astro.url.pathname.endsWith('/') ? Astro.url.pathname : `${Astro.url.pathname}/`;
---

<PageSection role="nav">
    <nav class="navbar">
        <h1 set:html={title}/>
        <div class="links">
            {
                links.map((item) => (
                    <a class={`link font-mono ${url === item.href ? 'link-active' : ''}`} href={item.href}>{item.i}</a>
                ))
            }
        </div>
    </nav>
</PageSection>

<style>
    .navbar {
        display: flex;
        flex-direction: column;
        align-items: start;
        justify-content: start;
        .links {
            font-size: 16px;
            display: flex;
            flex-direction: row;
            align-items: center;
            justify-items: start;
            gap: 0.75rem;
            padding: 0.35rem 0.75rem;
            border-radius: var(--border-radius);
            background-color: rgba(var(--primary-color), 0.05);
            flex-wrap: wrap;
        }
    }
</style>